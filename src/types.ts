/**
 * Root-level type re-exports for convenience.
 *
 * The canonical type definitions live in @apicraft/core.
 * This file ensures backwards compatibility with imports from the root.
 *
 * @see @apicraft/core for full type definitions
 */
export type { defineConfig as defineConfigType } from "../packages/core/src/config.js";
